# API base URL. Empty in dev → requests go to "/api", which Vite proxies to
# http://127.0.0.1:8000 (see vite.config.ts). Set this to the real backend
# origin when deploying the built frontend separately from the API.
VITE_API_URL=
